FI-2010 数据集格式特征总结本研究采用的 FI-2010 基准数据集在时间和空间维度上具有极高的规律性，其核心格式特点可以总结如下：时序与数据划分（训练/测试集）：数据集共包含 10 个连续交易日 的限价订单簿（LOB）数据。依据标准的滚动交叉验证设计，前 7 天 的数据（Train...CF_7.txt）被归为模型开发集，在代码中按 8:2 的比例切分为训练集（约 5.6 天）与验证集（约 1.4 天）；后 3 天 的数据（Test_CF_7/8/9）则通过横向拼接（np.hstack）合并为一个完整的独立测试集，用于进行样本外（Out-of-Sample）的终极性能与对抗攻击评估。时间轴维度（列/Columns）：矩阵的每一列代表一个 Tick（滴答）。Tick 是高频交易中订单簿状态发生更新（如新挂单、撤单、成单）时的基础时间快照，每一列即为该特定瞬间的市场状态。空间与标签维度（行/Rows）：每个 Tick 包含固定 45 行 数据，从上至下严格划分为特征和标签两部分：前 40 行（0 至 39 行）为 10 档限价订单簿特征：数据按照“档位由浅入深（Level 1 到 Level 10）、卖方优先、价量交替”的嵌套循环顺序纵向排列。具体为：卖一价 -> 卖一量 -> 买一价 -> 买一量 -> 卖二价 ... -> 买十量  这种紧凑的交织格式能够完美保留订单簿的空间微观结构。后 5 行（40 至 44 行）为预测视野分类标签：存放基于未来平滑均价计算出的价格趋势标签（1 涨、2 平、3 跌）。在 DeepLOB 专属的预处理数据中，这 5 行（代码索引 k=0 至 k=4）分别精确对应未来 10、20、30、50 和 100 个 Ticks 的预测视野，为模型在不同预测深度下的回测提供了标准答案。

Databeto 的原始数据是以逐笔事件（Event-by-Event）为基础高频流动的。为了严格协调 FI-2010 格式，数据清洗没有采用跨时间求平均或聚合的逻辑，而是引入了步长为 10 的下抽样（Subsampling / Stride=10）机制。即在连续的时间轴上，每发生 10 个订单簿基础事件更新，我们只抓取第 10 个事件发生那一瞬间的订单簿静态快照（在 Python 中表现为 [9::10] 切片），直接丢弃中间的 9 个状态。抽样后的矩阵横向转置为列，每一列即代表一个对齐后的标准 Tick。



